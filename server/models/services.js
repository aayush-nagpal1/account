'use strict';

module.exports = function(Services) {
    Services.fetchCreditAccounts = function(cb){
        cb(null);
    }

    Services.fetchDebitAccount = function(cb){
        cb(null);
    }

    Services.fetchTotalBalances = function(username,token,cb){
        
        cb(null);
    }

    Services.saveCustomizedMinBalance = function(cb){
        cb(null);
    }

};
